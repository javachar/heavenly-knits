// app/uc/layout.js
export const metadata = {
  robots: { index: false, follow: false },
  title: 'Under Construction — Heavenly Knits',
};

export default function UcLayout({ children }) {
  return children;
}
